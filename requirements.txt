# HRS 桌面端与爬虫依赖（Python 3.10+ 建议）
# 安装：python -m pip install -r requirements.txt
# 推荐先建虚拟环境：python -m venv .venv && source .venv/bin/activate

customtkinter>=5.2.0
google-genai>=1.0.0
PyMuPDF>=1.24.0
Pillow>=10.0.0
python-docx>=1.1.0
Jinja2>=3.1.0
requests>=2.31.0
selenium>=4.15.0
